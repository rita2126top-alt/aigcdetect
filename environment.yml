name: aigcdetect
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pytorch=2.5.1
  - torchvision=0.20.1
  - torchaudio=2.5.1
  - pytorch-cuda=12.1
  - numpy
  - pillow
  - scikit-learn
  - pyyaml
  - tqdm
  - pip:
      - loguru
      - ftfy
      - regex
      - huggingface_hub[cli]
      - pytest
      - datasets
      - tensorboard
